<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head'); %>
</head>

<body class="container">

    <header>
        <%- include('../partials/header'); %>
    </header>

    <main>
        <div class="jumbotron">
            <h1>Bienvenido <%= data[0].name%></h1>
            <p>Esta es la web para visualizar los datos de seguimiento de la conservacion de su pescado</p>
        </div>
        <section class="box">
            <label class="box-title" for="cb1">Información de assets</label>
            <label class="box-close" for="acc-close"></label>
            <div class="box-content">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <!--<th>Number of Transactions</th>-->
                            <th>Asset</th>
                            <th>Fecha</th>
                            <th>Temperatura</th>
                        </tr>
                    </thead>

                    <tbody class="information">
                        <% if (data) { %>
                            <% for (var i=0; i < data.length; i++) { %>
                                <tr>
                                    <!--<td>
                                    <%= (i+1) %>
                                </td>-->
                                    <td>
                                        <%= data[i].nombre%>
                                            <%= data[i].apellido%>
                                    </td>
                                    <td>
                                        <%= data[i].email %>
                                    </td>
                                    <td>
                                        <%= data[i].cumpleaños %>
                                    </td>
                                    <td>
                                        <%= data[i].genero %>
                                    </td>
                                </tr>
                                <% } %>
                                    <% } %>
                    </tbody>
                </table>
            </div>
        </section>
    </main>

    <footer>
        <%- include('../partials/footer'); %>
    </footer>

</body>

</html>